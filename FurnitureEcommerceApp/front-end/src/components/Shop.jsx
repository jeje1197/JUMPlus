const Shop = ({ user }) => {

    

    return (
        <div id="shop">
            <h3>Welcome, {user?.name}!</h3>
            <h5>Shop for all the hotest items</h5>

            {

            }
            
        </div>
    )
}

export default Shop
